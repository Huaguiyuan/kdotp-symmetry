language: python
cache: pip
python:
    - "3.6"

install:
    - pip install .
    - pip install mypy

# run tests
script:
    - mypy --ignore-missing-imports kdotp_symmetry
    - cd tests
    - py.test
